# Minimal broker config for the optional `mqtt` compose profile (local/demo use only).
# Anonymous access is enabled for convenience — put a password_file in front of it before
# exposing the broker beyond a trusted network.
listener 1883
allow_anonymous true
persistence false
