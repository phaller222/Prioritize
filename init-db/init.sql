-- Erstellt das Schema für Flowable, falls es noch nicht existiert
CREATE SCHEMA IF NOT EXISTS prioritize_flowable;
-- Optional: Falls du noch andere Schemas nutzt
-- GRANT ALL ON SCHEMA prioritize_flowable TO prioritize;