# Test-only overrides, merged on top of the main application.yaml (different filename, so it does
# not shadow it). Disables the background task-schedule poller so no scheduling thread runs during
# @SpringBootTest contexts. The scheduling logic itself is covered by pure unit tests.
prioritize.scheduling.enabled=false
